@* @model YourNamespace.Models.Article *@

<h1>@Model.Title</h1>
<p class="text-muted">@Model.CreatedAt.ToString("dd.MM.yyyy")</p>

<div>
    @Html.Raw(Model.Content)  <!-- pokud máš obsah v HTML -->
</div>

<a asp-action="Articles">← Zpět na seznam článků</a>
